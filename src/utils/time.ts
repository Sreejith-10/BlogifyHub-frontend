
// export const getTime = (sentT: string): string => {

	// Example usage:
	// //getting date
	// // let postT = new Date(sentT);
	// //getting current date
	// // let now = new Date();
	// //difference between two dates
	// // let difference = Math.abs(Math.floor((now.getTime() - postT.getTime()) / 1000));

	// let now = new Date().toLocaleString()

	// console.log(now);
	// console.log(new Date(sentT).toLocaleString());

	// const current = moment(now)
	// const postT = moment(sentT)
	// // console.log(current);
	// // console.log(postT);

	// const difference = postT.diff(current, "seconds");
	// console.log(difference);

	// if (difference < 60) {
	// 	return `posted ${difference} seconds ago`;
	// } else if (difference < 3000) {
	// 	const hours = Math.floor(difference / 60);
	// 	return `posted ${hours} minute${hours > 1 ? "s" : ""} ago`;
	// } else if (difference < 86400) {
	// 	const hours = Math.floor(difference / 3600);
	// 	return `posted ${hours} hour${hours > 1 ? "s" : ""} ago`;
	// } else {
	// 	const days = Math.floor(difference / 86400);
	// 	return `posted ${days} day${days > 1 ? "s" : ""} ago`;
	// }
// };
